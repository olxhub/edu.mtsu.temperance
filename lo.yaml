# lo.yaml — repository metadata for the author catalog (read by get_repositories).
#
# Git conventions come first: the catalog takes the description from README.md
# and licensing/attribution from LICENSE.TXT / CONTRIBUTORS.TXT. This file is
# ONLY for fields those conventions don't cover.
#
# NOT a deployment manifest — routes/namespace live in manifest.yaml.

title: MTSU Temperance Module (READ 1000)
discipline: Reading
